#!/usr/bin/env bash
# Thin Unix entrypoint — delegates to scripts/dev-cycle.ts via mise-managed
# tsx. Shares all logic with the Windows entrypoint (scripts/dev-cycle.cmd).
set -euo pipefail
DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$DIR"
exec mise exec -- tsx "scripts/dev-cycle.ts" "$@"
