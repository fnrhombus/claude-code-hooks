# Blockers

Long-standing issues the automated type-sync pipeline (`scripts/dev-cycle`) couldn't handle on its own.

Each entry links to a tracking GitHub issue. If you're looking at the auto-generated header on line 1 of [`packages/core/src/types.ts`](./packages/core/src/types.ts) and it doesn't match the current upstream hash, one of these is probably why.

---

<!-- The pipeline appends new entries here. Do not reorder. -->

_(none yet)_
